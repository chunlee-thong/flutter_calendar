name: flutter_calendar
description: A calendar widget that uses Material Design.
version: 1.0.0
author: Chunlee Thong
homepage: https://github.com/chunlee-thong/flutter_calendar

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.16.0
  tuple: ^1.0.1
  date_utils: ^0.1.0
  flutter_icons: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
